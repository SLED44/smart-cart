# SmartCart — Python Dependencies
# Install with: pip3 install -r requirements.txt

# Streamlit — the entire UI layer (>=1.37 for st.dialog, used by the
# recipe-preview popup on the propose/swap screens)
streamlit>=1.37.0

# HTTP requests — Kroger, Anthropic, and Supabase REST calls
requests>=2.31.0

# Anthropic Python SDK — Claude API (list parsing, product matching)
anthropic>=0.40.0

# Environment variable loading from .env file
python-dotenv>=1.0.0
